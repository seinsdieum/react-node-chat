import React from 'react';
import './WindowBar.css'
const WindowBar = ({...props}) => {
    return (
        <div {...props} className={'window__bar'}>

        </div>
    );
};

export default WindowBar;