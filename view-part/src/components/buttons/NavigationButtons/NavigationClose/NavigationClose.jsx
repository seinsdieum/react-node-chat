import React from 'react';
import './NavigationClose.css'

const NavigationClose = ({...props}) => {
    return (
        <button {...props} className={'nav__close'}>

        </button>
    );
};

export default NavigationClose;